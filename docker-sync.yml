options:
    verbose: true

syncs:
    data:
        src: '.'
        dest: '/srv'
        sync_strategy: 'unison'
        sync_host_port: 10873
        sync_userid: '33'
        #sync_user: 'www-data'
        #sync_groupid: '33'
        #sync_group: 'www-data'
        sync_args: '-prefer newer -copyonconflict'
